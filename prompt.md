与此同时呢，我希望在 Rust 里面这个包呢，它多一些开关。首先是三层 mipmap 的这个开关。你可以让它在开关里面决定保留哪些层。默认就保留最精细的层。然后再加一个开关，就保留三种 mipmap、，waveform、 spectrum 和 loudness其中的哪一些？默认只有 waveform。你在写好这个 Pyo3 的包之后，将其 push 到 GitHub 上面，注意不要把这个仓库当做主仓库的子仓库来处理。然后你在确保 Rust 这一片能运行了之后，你先尝试引用远端的，就是这个 Pyo3 的仓库，这个操作是可以在 PyProject 里面去做的。引用了远端的仓库，就可以在主仓库里面，注意还是 Pyo3 的 work tree 里面去做相应的测试。看一下 Python 的方式和 Pyo3 的方式的效率对比。要注意，你在刚开始测试的时候，一定要用生成一些比较小的文件去做 benchmark。只有在你确定所有工作都完成了之后，你再生成一个比较大的，比较复杂的，一个小时的 wave 文件，去完成这项工作。如果你觉得在主仓库的 Work tree 里面工作不方便的话，我可以允许你去再开一个，再在上面，再在上一层的文件夹里面再开一个仓库，然后在那里面工作，在那里面去开 Work tree，在那里面用 Subs agent 去并行完成任务。你可以先跟我说说你的想法。
